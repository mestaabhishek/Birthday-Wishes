<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".CelebrationsPage">

    <VideoView
        android:id="@+id/birthdayView"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        android:layout_alignParentStart="true"
        android:layout_alignParentLeft="true"
        android:layout_marginStart="0dp"
        android:layout_marginLeft="0dp"
        android:layout_marginTop="0dp"
        android:src="@android:drawable/ic_dialog_map"
        android:visibility="visible" />

    <include
        layout="@layout/video" />

    <VideoView
        android:id="@+id/crackersView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/birthdayView"
        android:layout_above="@id/nextButton"
        android:src="@android:drawable/ic_dialog_map" />

    <TextView
        android:id="@+id/wishesText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/nextButton"
        android:text="@string/wish_you_many_many_happy_returns_of_the_day_n_nsending_you_smiles_for_every_moment_of_your_special_day_have_a_wonderful_time_and_a_very_happy_birthday"
        android:textColor="#ffffff"
        android:textSize="25sp"
        android:textStyle="bold|italic"
        tools:background="@android:color/transparent" />

    <Button
        android:id="@+id/nextButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@drawable/roundbutton"
        android:text="@string/next"
        android:onClick="onClickNextButton"
        android:textStyle="bold" />

</RelativeLayout>